class Circle {
    init(radius) {
        this.radius = radius;
    }

    area {
        return Math.PI * this.radius * this.radius;
    }
}

var circle = Circle(4);
print circle.area; // expect: 50.26548245743669

