var str = "hello";
var length = String.length(str);
for (var i = 0; i < length; i = i + 1) {
    print String.substr(str, i, i + 1);
}

// expect: h
// expect: e
// expect: l
// expect: l
// expect: o