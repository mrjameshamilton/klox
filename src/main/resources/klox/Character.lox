class Character {
    static toCharCode(char) {
        System.fail(Error("Native implementation for 'toCharCode' not provided."));
    }

    static fromCharCode(n) {
        System.fail(Error("Native implementation for 'fromCharCode' not provided."));
    }

    static rangeTo(start, end) = CharacterRange(start, end)
}
